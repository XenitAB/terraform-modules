apiVersion: v1
kind: Service
metadata:
  name: {{ include "aks-core-extras.fullname" . }}
  labels:
    {{- include "aks-core-extras.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aks-core-extras.selectorLabels" . | nindent 4 }}

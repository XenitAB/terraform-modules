apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cert.fullname" . }}-test-connection"
  labels:
    {{- include "cert.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cert.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

apiVersion: v1
kind: Service
metadata:
  name: {{ include "cert.fullname" . }}
  labels:
    {{- include "cert.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cert.selectorLabels" . | nindent 4 }}
